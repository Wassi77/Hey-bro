# Hey-bro

This repository contains a simple Flask based file sharing website.

## Requirements

- Python 3
- Flask

Install dependencies with:

```bash
pip install -r requirements.txt
```

## Running

Start the application using:

```bash
python app.py
```

The site will be available at `http://localhost:5000`. Upload files and download them from the list.
